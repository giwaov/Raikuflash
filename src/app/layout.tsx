import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "The Flash - Lightning Fast Solana Swaps",
  description: "Sub-50ms swap confirmations on Solana. Win high-volatility events with guaranteed transaction inclusion via Raiku JIT.",
  keywords: ["Solana", "DeFi", "Swap", "Jupiter", "Raiku", "JIT", "HFT"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
